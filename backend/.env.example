# Copy to .env for local development — never commit .env itself
# ALLOWED_ORIGINS=http://localhost:3000
# MODEL_PATH=model_weights/ghostnet.onnx
# MODEL_DOWNLOAD_URL=
# ENVIRONMENT=development
# DEBUG=true
# CONFIDENCE_THRESHOLD=0.4
